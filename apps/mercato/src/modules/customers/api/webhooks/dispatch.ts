/**
 * @deprecated — moved to @open-mercato/core/modules/webhooks/lib/dispatch.ts
 * in SPEC-062 Phase 1. This re-export exists for backwards-compat with
 * callers inside the app. Update imports when touching adjacent code.
 */
export { dispatchWebhook, sendTestDelivery } from '@open-mercato/core/modules/webhooks/lib/dispatch'
